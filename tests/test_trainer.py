import numpy as np

import pytest

from models import LogisticRegression, SimpleNN, ComplexNN
import trainer

#test plan

#1. build model

#2. train model

#3. (if time) add training over epochs to see loss reduction
# the datasets are small and noisy so this could be useful